# Match-3 游戏玩法说明

本文档描述 **控制台版** 与 **Web 版** 的通用规则，实现以 `Assets/Scripts/`（C#）与 `web/src/core/`（TypeScript）为准。

---

## 1. 游戏概述

Match-3（三消）是一种益智类游戏。通过**交换两个相邻元素**，使同一行或同一列出现**至少 3 个连续相同元素**即可消除得分。

当前版本支持：

- **难度递进**：通关后元素种类增加、棋盘变大
- **关卡目标**：达到目标分数进入下一关
- **特殊元素**：炸弹（3×3 爆炸）、冰块（障碍，邻格消除时碎裂）
- **排行榜**：记录历史最高分

---

## 2. 棋盘与元素

### 2.1 棋盘大小（随关卡变化）

| 关卡 | 棋盘 | 元素种类 |
|------|------|----------|
| 1 | 6×6 | 3 |
| 2 | 6×6 | 3 |
| 3 | 7×7 | 4 |
| 5 | 8×8 | 5 |
| … | 最大 10×10 | 最大 6 |

### 2.2 符文元素

| 控制台符号 | Web 显示 | 颜色 |
|------------|----------|------|
| 火系符文 | F | 红 |
| 水系符文 | W | 蓝 |
| 自然符文 | N | 绿 |
| 雷电符文 | L | 黄 |
| 光系符文 | 浅白 | 白 |
| 暗影符文 | S | 紫 |

### 2.3 特殊元素

| 元素 | 控制台 | Web | 规则 |
|------|--------|-----|------|
| 炸弹 | `B` | 红色炸弹图 | 可参与三连；消除时引爆周围 3×3 |
| 冰块 | `#` | 蓝色冰块图 | **不可交换**；**不能三连**；仅当相邻格被消除时碎裂 |

---

## 3. 基本操作

### 控制台版

```powershell
dotnet run --project Match3Console.csproj
```

输入相邻两格坐标交换：`x1 y1 x2 y2`  
其他命令：`hint` 提示 · `auto` 自动一步 · `r` 排行榜 · `q` 退出

### Web 版

```powershell
cd web && npm run dev
```

点击两格交换 · 按钮 Hint / Auto / Restart

---

## 4. 交换规则

| 条件 | 说明 |
|------|------|
| **相邻** | 上下或左右相邻，不能斜换 |
| **可交换** | 冰块不可作为交换方 |
| **产生匹配** | 交换后至少形成一组三连（符文或炸弹） |

无效交换会被撤销（Web 底部提示 `Invalid swap`）。

---

## 5. 匹配与消除

### 5.1 匹配

同一行或列上 **3 个及以上**连续相同符文/炸弹构成匹配。横向与纵向可同时存在，合并消除。

### 5.2 消除流程

```
检测匹配 → 清除格子 → 炸弹扩 3×3 → 碎裂相邻冰块
    → 重力下落 → 顶部填充 → 再次检测（连锁）→ 结束
```

### 5.3 冰块说明

冰块**不能**通过「三个冰块连成一线」消除。  
只有当冰块**上下左右紧邻**的格子参与消除时，该冰块才会碎裂消失。

---

## 6. 计分与关卡

### 6.1 得分

- 每消除 1 格：**10 分**（基础分，可配置）
- 连锁每轮分别计分

### 6.2 关卡目标

| 字段 | 含义 |
|------|------|
| Goal 分数 / 目标分 | 当前关进度 / 通关所需分数 |
| Total | 历史累计总分（不清零） |
| Level | 当前关卡 |

目标分公式：`400 + 关卡 × 350`（第 1 关 750 分）

### 6.3 通关

当前关分数 ≥ 目标分 → 进入下一关：

- 关卡 +1，当前关分数清零
- 棋盘尺寸、元素种类按关卡规则提升
- 重新生成棋盘（含新数量冰块）

---

## 7. 游戏结束

当棋盘上**不存在任何合法交换**时 Game Over，记录总分并更新排行榜。

Web 版在 Game Over 时自动写入 `localStorage`；控制台写入 `highscores.txt`。

---

## 8. 策略提示

1. 优先制造**连锁**，一次交换多轮得分
2. 炸弹参与三连可触发更大范围清除
3. 冰块旁消可逐步打开局面，但冰块本身不能拖动
4. 关卡越高棋盘越大、元素越多、冰块越多，死局概率上升
5. 无路可走时用 Hint；Web 的 Auto **每次只走一步**

---

## 9. 当前版本限制

- 无消除/下落动画与音效
- 无冰块碎裂特效
- 无限时、限步模式
- Auto 非连续自动挂机

---

## 10. 项目与 AI 协作说明

本仓库由开发者主导、Cursor AI 结对完成 Web 演示层建设，包括逻辑移植、Canvas UI、扩展玩法与 AI 资产生成。详见：

- [README.md](./README.md) — 项目总览与 **AI 协作方法论**（面试向）
- [web/README.md](./web/README.md) — Web 工程分工与典型案例

---

## 11. 快速参考

| 项目 | 值 |
|------|-----|
| 最少匹配数 | 3 |
| 单格得分 | 10 |
| 第 1 关目标 | 750 |
| 冰块 | 不可交换，邻格消除碎裂 |
| 炸弹 | 消除时 3×3 爆炸 |
